# 엑셀 입력 폴더

최신 개인정보 제외 엑셀 파일(`.xlsx`)을 이 폴더에 넣고 프로젝트 루트의
`update_data.cmd`를 실행합니다. 파일이 여러 개면 수정 시각이 가장 최근인
파일 하나를 사용합니다.

이 폴더의 파일은 `.gitignore`에 의해 GitHub에 올라가지 않습니다. GitHub Pages는
별도의 공개 파일 허용 목록으로 만들어지므로 이 폴더에는 접근하지 않습니다.
